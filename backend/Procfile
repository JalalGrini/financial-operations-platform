# Railway / Nixpacks process file. Root Directory must be `backend`.
# Railway injects PORT; bind all interfaces so the proxy can reach gunicorn.
web: gunicorn config.wsgi:application --bind 0.0.0.0:$PORT --workers 2 --timeout 120
