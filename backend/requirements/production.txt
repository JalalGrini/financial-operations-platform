# Production Requirements
# Supported Python: >=3.13,<3.15 (3.14 preferred for production)
# Generated: 2026-07-14
# Rule: Do not upgrade major versions without explicit approval and compatibility verification.

# Base requirements
-r base.txt

# Production WSGI/ASGI Server
gunicorn==23.0.0

# Serve STATIC_ROOT on Railway (no nginx in front of gunicorn).
whitenoise==6.9.0

# Object storage is mandatory in production settings (USE_S3_STORAGE=True).
-r storage-r2.txt
