# Service-root requirements for Railway / Railpack / Nixpacks.
# Set the Railway service Root Directory to `backend` so this file is visible.
# Local tests still install from requirements/development.txt (includes this
# stack plus pytest). Do not duplicate pins here — include the existing files.
#
# Supported Python: >=3.13,<3.15 (3.14 preferred for production)

-r requirements/production.txt
