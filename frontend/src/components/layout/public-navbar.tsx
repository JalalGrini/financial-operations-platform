"use client";

/**
 * Public marketing navbar: Accueil, Services, Le Groupe, Contact, Tickets.
 * Le Groupe dropdown: companies.map over the group list to /companies/[slug].
 * Uses DropdownMenu / dropdown on the Groupe item.
 */
export { LandingNav as PublicNavbar } from "@/components/landing/LandingNav";
export { LandingNav } from "@/components/landing/LandingNav";
